# Proxy API calls to the FastAPI backend (replace with your Render URL)
/api/*  https://voult-backend.onrender.com/api/:splat  200

# SPA fallback — every other route serves index.html
/*  /index.html  200
